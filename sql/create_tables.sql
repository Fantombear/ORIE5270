CREATE TABLE IF NOT EXISTS trading_:d (
    id serial primary key,
    A varchar,
    B varchar
);

CREATE TABLE IF NOT EXISTS temporal_trading(
    id serial primary key,
    A varchar,
    B varchar
);
